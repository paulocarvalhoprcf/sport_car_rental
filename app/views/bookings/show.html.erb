<p>teste booking show</p>

<ol class="list-group-item">
<li>
      <p><%= @booking.starting_date %> <%= @booking.ending_date %></p>
      <%= link_to "Delete date",
                  booking_path(@booking),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %>
      <%= link_to "Update",
                  edit_booking_path(@booking),
                  data: { confirm: "Are you sure?" } %>
      <%= link_to 'Back to cars', cars_path %>
    </li>
</ol>
